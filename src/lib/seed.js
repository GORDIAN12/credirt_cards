// Datos de ejemplo eliminados — la app usa Supabase como fuente de verdad.
// Este archivo se mantiene por si en el futuro se quiere repoblar la BD.
export function seedState() {
  return {
    tarjetas: [],
    compras: [],
    cuotas: [],
    pagos: [],
    adeudos: [],
    abonosAdeudo: [],
  };
}
